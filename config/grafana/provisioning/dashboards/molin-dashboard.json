{
  "annotations": {"list": []},
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "panels": [
    {
      "title": "API 调用量（24h）",
      "type": "timeseries",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "targets": [{"expr": "rate(molin_api_calls_total[5m])", "legendFormat": "{{agency}} - {{status}}"}],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0}
    },
    {
      "title": "各子公司执行次数",
      "type": "bargauge",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "targets": [{"expr": "molin_agency_executions_total", "legendFormat": "{{agency_id}}"}],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}
    },
    {
      "title": "当日 API 费用（CNY）",
      "type": "gauge",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "targets": [{"expr": "molin_daily_cost_cny"}],
      "gridPos": {"h": 4, "w": 6, "x": 0, "y": 8}
    },
    {
      "title": "SOP 质量评分趋势",
      "type": "timeseries",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "targets": [{"expr": "rate(molin_quality_score_sum[1h]) / rate(molin_quality_score_count[1h])", "legendFormat": "{{agency_id}}"}],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 12}
    },
    {
      "title": "记忆条目数",
      "type": "timeseries",
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "targets": [{"expr": "molin_memory_entries", "legendFormat": "{{type}}"}],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 12}
    }
  ],
  "schemaVersion": 39,
  "tags": ["molin", "v6.6"],
  "templating": {"list": []},
  "time": {"from": "now-24h", "to": "now"},
  "title": "墨麟AI 监控面板",
  "uid": "molin-v66",
  "version": 1
}
